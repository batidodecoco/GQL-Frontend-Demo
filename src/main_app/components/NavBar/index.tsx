import { ReactElement } from "react";

export default function NavBar(): ReactElement {
  return (
    <nav className="my-4">
      <h1 className="font-bold text-2xl">GraphQL Talk</h1>
    </nav>
  )
}